import 'package:flutter/material.dart';

class ImageDetailPlant extends StatelessWidget {
  const ImageDetailPlant({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}